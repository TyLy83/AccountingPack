@model AccountingPack.Models.Business

@if (Model != null)
{


}
else
{

    <div class="col-xs-12 col-sm-12 col-md-4 col-lg-4">
        <div class="thumbnail">
            <div class="caption">
                <p class="pull-right"><a href="#"><i class="fa fa-gear"></i></a></p>
                <p class="text-center"><i class="fa fa-briefcase fa-2x"></i></p>
                <h4 class="text-center">Business</h4>
                <p class="text-center">
                    <button class="btn btn-info btn-xs"
                            data-url="@Url.Action("Create","Business")">
                        Update
                    </button>
                </p>
            </div>
        </div>
    </div>

}

